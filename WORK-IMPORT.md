# 作品更新说明

作品源目录：`E:\2026-作品集\work`。导入只读取此目录，不修改原文件。

## 作品目录

- `campaign-design`：每个项目使用独立文件夹，例如 `鳴潮x壽司郎聯動`。
- `store-visuals`：按作品命名排序，页面按项目分组。
- `advertising-banners`、`social-campaign`：年份由近到远排序，同年份按作品文件名自然排序，未知年份置于末尾。统一网格展示，每张图下方显示项目、分类、地区和年份。

建议命名：`分类_项目名称_地区_年份_detail_01.webp`。
Campaign 可加入用途：`campaign-design_鳴潮x壽司郎_kv_tw_2024_detail_01.webp`。
用途识别：`kv` 为主视觉；`社群` 为社群图；`周邊` 或 `周边` 为周边延展。
周边品类用括号标记，例如 `周邊延展（徽章）`；同项目同品类会放在一个灰底展示框内。
不符合命名的文件仍会导入，但地区和年份可能显示为 `GLOBAL`、`—`，应补充源文件命名。
不需要在网站展示、但需要保留在源目录中的图片，记录在 `scripts/work-exclusions.json`；重新导入时会自动跳过。

## 封面目录

实际封面目录为 `E:\2026-作品集\work_cover`（与 `work` 平级），下设四个分类目录。
按现有命名 `分类_项目名称_地区_年份_cover_01.webp` 导入；同项目归为一组，编号决定图片顺序。兼容重复的 `.webp.webp` 扩展名，也支持 PNG、JPG、JPEG、AVIF。
默认显示第一张，鼠标悬停时交叉淡化到第二张；仅一张图的项目保持静态。角标按钮可切换该项目其余封面。
广告固定展示以閃亮之名、運命逆転、갈락티코、ワルキューレの試練四个项目，顺序与此一致；以 1:1 排为桌面四栏、移动双栏。
其他分类封面为 16:9，桌面同时显示两张、移动一张，通过左右按钮或滑动查看其余项目。
封面标题集中在 `lib/work-covers.ts` 中编辑，不显示原始文件名。

“鳴潮x阿奇儂”封面使用 `work/campaign-design/鳴潮其他活動圖` 中名称含 `鳴潮x阿奇儂聯動` 的图片，按编号 01、02、04 顺序展示。此项目的源目录映射在 `scripts/cover-sources.json` 中维护，后续同命名的新图片也会自动加入；不会混入该目录内的其他活动项目。
只更新这一版位时，运行 `scripts/import_work_covers.py --only campaign-design-02`，其他封面保持不变。完整封面导入同样会读取这份映射。

## 更新与原图

维护时运行 `scripts/import_work_images.py` 更新作品；运行 `scripts/import_work_covers.py` 更新封面。
作品清单生成到 `lib/work-images.ts`，页面展示图片位于 `public/work`，保持原比例并懒加载。
所有二级作品页面不提供点击放大、查看原图或 100% 原尺寸功能，不请求高清原文件。导入脚本只生成展示图片，不再复制高清原文件到网站发布资源。
高清源文件仍保留在 `E:\2026-作品集\work`，不会修改；旧网站原图副本已移到本地 `outputs/original-image-backup`，不随网站发布，需要时可以恢复。
透明图自动使用 `#e9e9e9` 底色。网站不会展示 `sourceName` 原始文件名。
Campaign 社群图中的 16:9 图片使用与 1:1 图片相同高度的展示框，并保持完整画面、不缩放裁切内容。
封面目录不完整时，封面导入会报错并保留原有清单，避免误发布。
